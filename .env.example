# === Frontend / NextAuth ===
NEXTAUTH_URL=https://carteado.ferdinandes.com.br
NEXTAUTH_SECRET=replace_with_long_random_string
AUTH_TRUST_HOST=true
GOOGLE_ID=your_google_oauth_client_id.apps.googleusercontent.com
GOOGLE_SECRET=your_google_oauth_client_secret

# JWT secret used in custom generateJWT (can match NEXTAUTH_SECRET or be different)
JWT_SECRET_KEY=replace_with_another_long_random_string

# === Backend ===
# Reuse JWT_SECRET_KEY for backend JWT_SECRET_KEY (docker-compose maps it)
# If you want a separate value, set JWT_SECRET_KEY in compose and put it here.

# === Optional Public URLs ===
NEXT_PUBLIC_API_URL=https://carteado.ferdinandes.com.br
SOCKET_URL=http://carteado.ferdinandes.com.br/socket.io/

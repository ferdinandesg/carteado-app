echo ">> Rodando quality gate leve antes do push (lint + typecheck)..."
npm run prepush
if [ $? -ne 0 ]; then
  echo "Qualidade falhou. Corrija antes de fazer push."
  exit 1
fi

echo "Push liberado!"
